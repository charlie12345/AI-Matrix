# AI-Matrix

Static benchmark dashboard for the local `build-fusion` llama.cpp runtime stack.

This site is intended for GitHub Pages hosting and contains only:

- `index.html`
- `app.js`
- `styles.css`
- `results-data.js`

No backend, auth, uploads, or server-side code are required.
